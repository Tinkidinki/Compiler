class Program{
    int b;
    
    void add(int x, int y){
        return x + y;
    }

    void main(){
        int a;
        int b;
        a = callout("read", "Enter first number:");
        b = callout("read", "Enter second number:");
        callout ("printf", callout("add", a, b));
    }
}

